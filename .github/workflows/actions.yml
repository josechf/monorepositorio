name: repartidorpipsas

on:
  push:
    branches:
      - main

jobs:
  build-web:
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # Necesario para OIDC
      contents: read   # Necesario para actions/checkout
    steps:
      - name: checkout repo
        uses: actions/checkout@v4.2.2

      - name: configurar aws
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::418272787842:role/repartidor
          aws-region: us-east-1
      
      - name: configurar flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.29.2"

      - name: Instalar dependencias
        run: |
          flutter pub get
          flutter doctor -v
            
      - name: Construir Flutter Web (con logs detallados)
        run: |
          flutter config --enable-web
          flutter clean
          flutter build web --release -v 2>&1 | tee build.log
            
      - name: Mostrar logs de error
        if: failure()
        run: |
          cat build.log || echo "No se encontró archivo de log"
          ls -la || echo "No se pudo listar directorio"
          pwd || echo "No se pudo obtener directorio actual"
            
      - name: Subir build web a S3
        if: success()
        run: |
          aws s3 sync build/web s3://webflutters3 --delete \
            --cache-control "no-cache, no-store, must-revalidate" \
            --exclude "*" \
            --include "assets/*" \
            --include "manifest.json" \
            --include "*.html" \
            --include "*.js" \
            --include "*.wasm"
      - name: Debug - confirm upload to S3
        run: echo "Upload to S3 completed"
        
  deploy-lambda:
    needs: build-web
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - name: checkout repo
        uses: actions/checkout@v4.2.2

      - name: configurar aws
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::418272787842:role/awsgithubactions
          aws-region: us-east-1
      # lambda
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install dependencies
        run: npm install
      - name: deploy lambda
        #run: npx serverless deploy function -f api
        run: npx serverless sls deploy
      